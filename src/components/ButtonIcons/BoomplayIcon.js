import React from 'react';

const BoomplayIcon = ({ style }) => {
  return (
    <img
      src="/imgs/Boomplay-Music.png"
      alt="Boomplay Icon"
      style={{ width: '1em', height: '1em', ...style }}
    />
  );
};

export default BoomplayIcon;
